# EN: Store port groups that the scanner and AI use.
# VI: Cất các nhóm cổng để máy quét và AI dùng.

HIGH_RISK_PORTS = {21, 22, 23, 25, 53, 69, 80, 110, 111, 135, 137, 138, 139, 143, 161, 389, 443, 445, 465, 514, 554, 587, 631, 993, 995, 1433, 1521, 1723, 1883, 2049, 2375, 2376, 27017, 3306, 3389, 5000, 5432, 5601, 5900, 5985, 5986, 6379, 6443, 8080, 8443, 9000, 9200, 11211}
VERY_HIGH_RISK_PORTS = {21, 23, 69, 111, 135, 137, 138, 139, 161, 445, 1433, 1521, 1883, 2049, 2375, 27017, 3306, 3389, 5432, 5900, 6379, 6443, 9200, 11211}
COMMON_WEB_PORTS = {80, 443, 5000, 5601, 8080, 8443, 9000}
REMOTE_ACCESS_PORTS = {21, 22, 23, 3389, 5900, 5985, 5986}
MAIL_PORTS = {25, 110, 143, 465, 587, 993, 995}
DB_PORTS = {1433, 1521, 27017, 3306, 5432, 6379, 9200}
FILESHARE_PORTS = {111, 137, 138, 139, 445, 2049}
MEDIA_SERVICE_PORTS = {554, 5000, 7000}

# ── Feature engineering ────────────────────────────────────────────────────────
UNCOMMON_PORT_MIN = 1024
CRITICAL_RISK_SCORE_MIN = 4
HIGH_RISK_SCORE_MIN = 3
EPHEMERAL_PORT_MIN = 49152

# ── Heuristic labeling ─────────────────────────────────────────────────────────
SUSPICIOUS_SCORE_THRESHOLD = 5
MIN_DB_COUNT_THRESHOLD = 2
MIN_FILESHARE_COUNT_THRESHOLD = 2
MIN_REMOTE_ACCESS_COUNT_THRESHOLD = 2
MIN_VERY_RISKY_COUNT_THRESHOLD = 3
MIN_CRITICAL_PORT_COUNT_THRESHOLD = 1
MIN_CLEARTEXT_COUNT_THRESHOLD = 2
MIN_ADMIN_PORT_COUNT_THRESHOLD = 2
MIN_OPEN_PORT_COUNT_THRESHOLD = 8
MIN_UNCOMMON_PORT_COUNT_THRESHOLD = 3
MULTI_COUNT_RISK_SCORE = 2
VERY_RISKY_COUNT_RISK_SCORE = 3

# ── ML model parameters ────────────────────────────────────────────────────────
MIN_SAMPLES_TO_TRAIN = 2
MIN_SAMPLES_FOR_STRATIFY = 2
MIN_SAMPLES_FOR_LR = 4
SMALL_DATASET_ROW_LIMIT = 3
TEST_SPLIT_SMALL_DATASET = 0.5
TEST_SPLIT_NORMAL_DATASET = 0.3
DT_MAX_DEPTH = 6
DT_MIN_SAMPLES_SPLIT = 2
DT_MIN_SAMPLES_LEAF = 1

# ── Severity probability thresholds ───────────────────────────────────────────
SEVERITY_CRITICAL_MIN_PROB = 0.98
SEVERITY_HIGH_MIN_PROB = 0.95
SEVERITY_MEDIUM_MIN_PROB = 0.85

# ── Unknown port confidence scores ────────────────────────────────────────────
CONFIDENCE_NO_INFO = 0.15
CONFIDENCE_EPHEMERAL_PORT = 0.35
CONFIDENCE_MACOS_DYNAMIC = 0.70
CONFIDENCE_APPLE_CONTINUITY = 0.65
CONFIDENCE_MEDIA_STREAMING = 0.60
CONFIDENCE_MEDIA_DYNAMIC = 0.50
CONFIDENCE_PRINTER = 0.55
CONFIDENCE_WEB_ADMIN = 0.30
CONFIDENCE_MAX = 0.95

# ── Nmap scan timing ──────────────────────────────────────────────────────────
NMAP_FAST_MIN_RATE = 2000       # packets/sec for phase-1 port discovery
NMAP_FAST_MAX_RETRIES = 1       # low retries — speed over completeness in phase 1
NMAP_SERVICE_MAX_RETRIES = 2    # reduced from 3; catches most services with less wait
NMAP_SERVICE_VERSION_INTENSITY = 7  # 7 = thorough but not exhaustive (vs --version-all = 9)

# ── Anomaly detection ─────────────────────────────────────────────────────────
ISOLATION_FOREST_CONTAMINATION = 0.15
ISOLATION_FOREST_N_ESTIMATORS = 100
ANOMALY_HIGH_THRESHOLD = 0.65

# ── Network pattern detection thresholds ──────────────────────────────────────
PATTERN_MIN_LATERAL_MOVEMENT_HOSTS = 2
PATTERN_MIN_SMB_CLUSTER_HOSTS = 3
PATTERN_MIN_CLEARTEXT_HOSTS = 2
PATTERN_MIN_DB_CLUSTER_HOSTS = 3
PATTERN_MIN_CONTAINER_HOSTS = 2
PATTERN_MIN_REMOTE_ACCESS_CLUSTER_HOSTS = 3
PATTERN_MIN_CRITICAL_MASS_HOSTS = 3

# ── Composite risk score weights (must sum to 100) ────────────────────────────
RISK_WEIGHT_ML_PROB = 40
RISK_WEIGHT_ALERT_SEVERITY = 30
RISK_WEIGHT_ANOMALY = 15
RISK_WEIGHT_BASELINE = 10
RISK_WEIGHT_ASSET = 5

# ── Label confidence ───────────────────────────────────────────────────────────
LABEL_CONFIDENCE_MIDPOINT = 0.5
LABEL_CONFIDENCE_MAX_BOOST = 0.5
LABEL_CONFIDENCE_DISTANCE_SCALE = 10.0

# ── Reporting ──────────────────────────────────────────────────────────────────
SCRIPT_OUTPUT_MAX_LENGTH = 140
PROGRESS_SPINNER_INTERVAL = 0.15
